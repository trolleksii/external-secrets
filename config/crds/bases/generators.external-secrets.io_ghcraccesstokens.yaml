apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: ghcraccesstokens.generators.external-secrets.io
spec:
  group: generators.external-secrets.io
  names:
    categories:
    - ghcraccesstoken
    kind: GHCRAccessToken
    listKind: GHCRAccessTokenList
    plural: ghcraccesstokens
    shortNames:
    - ghcraccesstoken
    singular: ghcraccesstoken
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              appID:
                description: Github App ID
                type: integer
              githubURL:
                default: https://api.github.com/
                description: Github API URL. Defaults to https://api.github.com/
                type: string
              installationID:
                description: Github App Installation ID.
                type: integer
              privateKeySecretRef:
                description: Secret referencet to the Github App private key
                properties:
                  key:
                    description: The key of the entry in the Secret resource's `data`
                      field to be used. Some instances of this field may be defaulted,
                      in others it may be required.
                    type: string
                  name:
                    description: The name of the Secret resource being referred to.
                    type: string
                  namespace:
                    description: Namespace of the resource being referred to. Ignored
                      if referent is not cluster-scoped. cluster-scoped defaults to
                      the namespace of the referent.
                    type: string
                type: object
              tokenLifetime:
                default: 10m
                description: TokenLifetime is the lifetime of the generated token.
                  Defaults to 10m. This is the maximum token lifetime allowed by Github.
                type: string
            required:
            - appID
            - githubURL
            - installationID
            - privateKeySecretRef
            - tokenLifetime
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
